const paintWalls = function (color, wallName) {
  console.log("The " + wallName + " has been painted " + color);
};

paintWalls("yellow", "north wall");
/* If we call the function without an argument,
the color will be printed as undefined */
